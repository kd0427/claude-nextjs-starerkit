// TODO: NextAuth.js v5 (Auth.js) 핸들러
// - src/lib/auth.ts 에서 export한 handlers를 여기에 연결
// - GET / POST 모두 처리

// TODO: import { handlers } from "@/lib/auth";
// export const { GET, POST } = handlers;

export async function GET() {
  // TODO: NextAuth.js v5 설치 후 위 주석 해제
  return new Response("Auth route - TODO", { status: 501 });
}

export async function POST() {
  // TODO: NextAuth.js v5 설치 후 위 주석 해제
  return new Response("Auth route - TODO", { status: 501 });
}
